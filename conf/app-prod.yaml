app:
  # database
  db:
    host: tcp(mysql:3306)
    password: dreamlu
    user: root
  log:
    #    const (
    #    Debug = "debug" // default, print all >= 'debug' level to file
    #    Info  = "info"
    #    Warn  = "warn"
    #    Error = "error"
    #    )
    level: error
  # redis
  redis:
    dB: 0
    addr: redis:6379
    poolSize: 0
    minIdleConns: 0
    #password:
  notifyUrl: https://your domain/api/v1/wx/notify